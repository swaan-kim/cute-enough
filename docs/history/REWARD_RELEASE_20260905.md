# 2026-09-05 광고·친구 초대·충전 알림 배포 기록

> 과거 구현·배포 기록입니다. 당시 상태·명령은 현재 운영 절차가 아닙니다. [문서 안내](../README.md)에서 현재 가이드를 확인하세요. 도감 초기화·승인·출시는 별도의 명시적 확인 없이 실행하지 않습니다.

이 문서는 확인한 기준과 남은 검증을 구분한다. 로컬 구현·테스트 통과가 커밋, 서버 배포, AIT 업로드 또는 출시 완료를 뜻하지 않는다. 뒤의 실행 기록에 실제 결과를 추가한다.

## 기준과 후보

| 구분 | 확인한 값 | 상태 |
| --- | --- | --- |
| 저장소 | `swaan-kim/cute-enough`, `main` | 작업 시작 HEAD `8566fe33929c3c65722b274c50e25f13feb95459` |
| Toss 앱 | workspace `17687`, miniAppId `67857`, appName `cute-enough` | 노출명 `옆집 강아지` |
| 운영 출시본 | `20260903-21` | deploymentId `01a065f2-ec98-797c-aa9c-55143c1ed436` |
| 출시본과 일치한 로컬 보관 파일 | `TO_UPLOAD/UPLOAD_FOR_TOSS_QR_TEST_cute-enough_2026-09-03.ait` | 1,244,829 bytes, 초기 JS gzip 486,892 bytes |
| 이번 보상 구현 전 최신 로컬 AIT | `cute-enough.ait` | deploymentId `01a07050-8037-792f-83e3-9bc3aa78a55f`, 1,196,981 bytes, 초기 JS gzip 432,003 bytes. 업로드 확인 없음 |
| 이번 변경의 운영 연결 확인용 AIT | `cute-enough.ait` | production, deploymentId `01a0715f-fa07-71a4-9570-6b56439395ec`, 1,201,437 bytes. 광고/공유/알림 비활성, 아직 콘솔 업로드 안 됨 |
| 운영 Supabase | `xoqjehiqbaxpalbghngm` | 작업 시작 확인 기준 `pet-api` version `15` |

출시본 보관 파일 SHA-256:
`6521631325993e48041826a50c81d30d59b2c1ac8fb8d7360cfd70218a9f2ecc`

보상 구현 전 최신 로컬 파일 SHA-256:
`4f06953f4fd57b349c62511318a871ecd037cb6a930c550d9fbc560f41c5f050`

두 AIT는 모두 광고 플래그가 꺼져 있고 광고 그룹 ID가 비어 있었다. 콘솔에서 확인한 기존 보상형 광고 슬롯은 `ait.v2.live.285b758f6d564708`이다. 새 AIT에 어떤 설정을 넣었는지는 빌드 결과와 함께 다시 기록한다.

## 작업 시작 시 운영/로컬 차이

- 운영 승인 7건, 대기 6건, 반려 2건을 읽기 전용으로 확인했다. 대기견은 별도 창에서 운영자가 수정할 예정이며 이 배포에서 수정하거나 승인하지 않는다.
- 운영 DB는 공개 5마리 상태였고 공유/광고 보상 원장과 알림 테이블은 아직 없었다. 로컬 목표는 공개 4마리 + 내 최신 pending/approved 1마리다.
- 정확 일치 검수 SQL은 운영에 수동 적용됐지만 마이그레이션 이력에는 없었다. 이력만 보고 SQL을 중복 적용하지 않는다.
- 과거 `20260829000100_fix_wooyoo_white_traits.sql`처럼 개별 강아지를 바꾸는 SQL은 자동 재실행하지 않는다.
- 기존 전체 업로드 이력 변경과 새 보상 API는 Edge Function 재배포 전까지 운영에 반영되지 않는다.
- Supabase CLI 재로그인과 프로젝트 연결은 완료했다. 인증 성공은 마이그레이션·함수 배포 성공과 별개다.

## 자동 검증 기록

| 범위 | 확인한 결과 | 최종 실행 기록 |
| --- | --- | --- |
| 알림 PostgreSQL/PGlite | 14/14 통과 | 자연 충전·보너스 제외·동의 해제·야간·일일 한도·선점 복구·불명확 결과 재전송 방지 |
| 알림 관련 Vitest | 6개 파일 35/35 통과 | 암호화·설정·SDK·응답 판정·worker |
| 보상 PostgreSQL/PGlite | 24/24 통과 | 원장, 중복 요청, 사용 순서, 광고 중 충전, 다중 공유 |
| 전체 Vitest / 검수 서버 / preflight | 72개 파일 434/434 / 12/12 / 4/4 통과 | 2026-09-05 실행 |
| TypeScript / Edge Deno 검사 | 모두 통과 | Deno 2.9.6, 로컬 npm 모듈로 서버 2개 검사 |
| 웹 / private·production AIT 빌드 | 모두 통과 | production SHA-256 `b32d1d48b776fafbeb832ca13b7a3ce08b40edab0e4f70f66284c68973c01b9d`, 1,201,437 bytes |

PGlite는 실제 SQL을 실행하지만 다중 연결 동시성이나 Toss 기기 동작을 대신하지 않는다. 실제 친구 초대·알림 발송을 자동 테스트에 넣지 않는다.

## Toss QR 검증표

각 항목에 기기(OS/앱 버전), AIT deploymentId, 사용자 A/B, 확인 시각과 결과를 남긴다. 미실행 항목을 통과로 바꾸지 않는다.

- [ ] 최초 무료 티켓 2장, 공개 4마리와 내 강아지 분리, 최대 5마리 움직임
- [ ] 3시간마다 티켓 1장 충전·최대 2장·장기 미접속·KST 자정 무료 초기화 없음
- [ ] 유효 재열람/내 강아지 → 해당 광고 보상 → 무료 → 공유 보너스 순서
- [ ] 최초 공개 실패·응답 유실·중복 요청에서 같은 요청 ID로 재시도하며 추가 차감 없음
- [ ] 사진 URL만 만료된 경우 유효 접근권으로 갱신하고 접근권 만료 시 자동 차감 없음
- [ ] 광고 시작은 무료와 보너스가 모두 0일 때만 가능
- [ ] 광고 완료 콜백 즉시 기록, 취소·실패 무차감, 하루 완료 2회 제한
- [ ] 광고 도중 자연 충전·완료 후 종료·서버 응답 유실·앱 재실행 후 보상 보존
- [ ] 무료 만충/오늘 완료 상태에서 친구 초대 보너스 적립 가능
- [ ] 초대 취소·친구 없음·여러 친구·같은 친구 재시도·모듈 재진입
- [ ] `sendViral` 수량과 `close` 집계 일치, 불일치는 추가 지급 없이 확인 상태
- [ ] 일반 강아지 공유/링크 복사에 보상 없음, 공유 보너스 자정·재실행 후 보존
- [ ] A 업로드 후 A 홈과 내 강아지 즉시 표시, B pending 공유는 캐릭터만 표시
- [ ] 운영자가 별도로 승인한 뒤 동일 공유 링크의 최종 디자인 갱신
- [ ] 승인 다음 KST 날짜부터 다른 사용자 공개 후보에 편입; 모든 사용자 필수 노출 조건은 아님
- [ ] 등록 재시도에서 사진·꾸밈·검수 결과 보존, 전체 등록 이력 페이지네이션
- [ ] 충전 알림 동의/거절/해제, 자연 0→1만 예약, 야간 보류, 하루 1회
- [ ] 충전 후 방문·오늘 공개견 완료·동의 해제 시 알림 제외, 중복 worker와 결과 유실 재발송 없음
- [ ] Android/iOS 및 기존 운영 AIT의 주요 API 호환

## 콘솔 확인과 알려진 제한

1. **공유 모듈 확인 필요.** `e5de3e72-cbfb-4b50-a050-9fd71fb4368b`가 해당 앱 소속이며 보상 단위 `강아지 티켓`, 친구당 수량 `1`인지 확인한다. 앞선 조회는 `Auth required`였지만, 현재 MCP 광고·알림 조회는 성공한다. 공유 목록(검색/전체)과 UUID 개별 조회 3경로는 모두 `존재하지 않는 정보 입니다`를 반환했다. ID가 잘못됐다고 단정하지 말고 콘솔에서 소속·실제 설정을 확인한다. 공유 MCP는 읽기만 지원한다.
2. **실제 공유 이벤트 검증 필요.** 공개 SDK에는 친구 ID·고유 지급 ID·검증 서명이 없고 서버 발송 이력 API를 확인하지 못했다. 수신한 콜백의 서버 요청 중복은 막지만 콜백 수신 전 앱 종료까지 완전 복구를 약속하지 않는다. 실제 친구에게 임의로 초대를 보내지 않는다.
3. **알림 동의문 문구와 QR 검증 필요.** 새 기능성 SERVER 템플릿 `cute-enough-ticket-recharged-v2` 승인과 서버 secrets/Vault 연결을 완료했다. termsId `120468`의 기존 동의문에는 이전 명칭이 남아 있어 `티켓`으로 수정해야 한다. MCP에는 동의문 수정 기능이 없어 콘솔 웹에서 확인한다. 지정 테스트 수신자의 실제 QR 검증까지 알림 스위치를 꺼 둔다. 일반 광고성 푸시 생성은 MCP 지원 범위 밖이며 기능성 알림과 별도로 콘솔 웹에서 준비한다.
4. **실기기 테스트 필요.** Vercel과 로컬 브라우저 테스트를 Toss QR A/B·광고·초대·알림 성공으로 간주하지 않는다. 대기견 승인은 운영자가 별도로 진행한다.
5. **미리보기 교체 필요.** 최종 QR 실제 화면으로 636×1048 PNG 4장과 1932×828 PNG 썸네일을 만든다. 홈에는 무료/보너스 잔액을 반영한다. 완성 이미지·설명문을 확인한 뒤 앱정보 검토를 요청한다.
6. **초기 JS 후속 개선.** 구현 전 최신 로컬 초기 JS는 약 432KB gzip으로 목표 350KB를 넘었다. 최종 빌드 수치를 다시 기록하되 코드 분할은 보상 안정화 이후 진행한다.

## 배포 실행 기록

| 단계 | 결과/증거 |
| --- | --- |
| 최종 검토·커밋·GitHub push | 구현 커밋 `76e0437`을 `origin/main`에 push 완료 |
| 운영 DB 이력 대조 및 필요한 SQL 적용 | 2026-09-05 20:43 KST. `20260905000100`은 실제 함수·뷰 대조 후 이력 등록, `00200`~`00500`은 단일 트랜잭션 적용. `pets` 전체 행 지문이 적용 전후 같음을 트랜잭션에서 검증. 과거 개별견 수정 SQL 미실행 |
| `pet-api` / 알림 worker 배포 | 2026-09-05 20:44 KST. `pet-api` version `16`, `recharge-notifications` version `1`, 둘 다 ACTIVE. 신규 티켓/등록 이력 API 운영 반영 |
| 배포 시점 광고/공유/알림 설정 | 20:44 KST 확인 당시 DB 광고·공유 모두 false. 알림 cron 매분 등록, worker 설정 false이므로 발송 호출 없음. 당시 알림 전용 secrets/Vault·동의문·템플릿 연결은 미완료; 이후 준비 결과는 아래 추가 기록 참고 |
| 운영 연결 확인용 AIT 생성 | production ID `01a0715f-fa07-71a4-9570-6b56439395ec`, 1,201,437 bytes, 초기 JS gzip 436235 bytes. 보상 활성화와 QR 결과에 따라 최종 제출 후보를 다시 확정 |
| AIT 콘솔 업로드·QR 생성 | 미완료. 콘솔 로그인 필요, 로컬 AIT 배포 credential 없음. 파일 생성만 완료 |
| 동일 AIT QR 검증 완료 | 미실행. 로컬 브라우저에서 무료/보너스와 4/4 홈 표시만 확인 |
| 최종 미리보기·설명문 확인 | `STORE_COPY_DRAFT.md` 문구·장면 초안 작성. 최종 QR 원본·PNG 4장·썸네일·콘솔 교체 미완료 |
| 심사 요청 / 승인 / 출시 | 이번 변경으로 요청·승인·출시하지 않음. 출시 기준은 계속 `20260903-21` |

배포 절차는 [앱인토스 제출 체크리스트](../operations/APPS_IN_TOSS_RELEASE.md), 알림 운영은 [티켓 충전 알림 운영](../operations/RECHARGE_NOTIFICATIONS.md)을 따른다. 새 기능을 중단해도 이미 지급한 보너스·광고 보상은 보존한다.

배포 후 확인: `pet-api`의 잘못된 액션 요청은 `404 INVALID_ACTION`과 `serverNow`를 반환했고, 인증 없는 알림 worker 요청은 `401 UNAUTHORIZED`로 거절했다. 실제 사용자 식별·사진·광고 보상·친구 초대·알림 발송의 Toss QR 검증을 대신하지 않는다. 승인 7건·대기 6건·반려 2건이 유지되었고 신규 보너스/공유 이벤트/알림 작업은 모두 0건이다.

## 이후 콘솔·서버 준비 기록 — 2026-09-05

- 광고 MCP 조회 성공: 기존 보상형 슬롯 `ait.v2.live.285b758f6d564708`은 `ENABLED`, 보상 1 `강아지 볼 기회`, abuse/restriction 없음이다. 새 슬롯을 만들지 않았고 앱·서버 광고 활성화는 하지 않았다.
- 공유 모듈은 위 3경로 조회에서 정보 없음이 반환됐다. 인증 실패와 구분하며, 소속·단위·수량·활성 상태·운영 기간은 콘솔 확인 전이다. 공유 신규 적립 스위치는 계속 `false`다.
- 조건형 동의문 termsId `120468`(subscription `7898`)을 생성했다. 현재 남아 있는 조건 문구 `무료 이용권이 충전되면 알려드릴까요?`를 비롯한 이전 명칭은 `티켓`으로 수정해야 한다. 이 수정은 아직 완료되지 않았다.
- 처음 승인된 `cute-enough-ticket-recharged`(groupNo `11799`, materialNo `13663`)는 승인 후 수정이 거절됐다. 사용자의 명칭 변경에 따라 **`cute-enough-ticket-recharged-v2`**를 새로 만들었고 MCP 재조회에서도 `APPROVED`를 확인했다. 새 값은 groupNo `11801`, materialNo `13665`, pushTemplateNo `99675`, termsId `120468`이다. 제목은 `티켓 충전`, 저장된 본문은 `강아지 티켓이 충전됐어요. (옆집 강아지 알림)`, 링크는 `intoss://cute-enough`다. 이전 템플릿은 사용하지 않고 이력으로 남긴다.
- 서버 `RECHARGE_NOTIFICATION_TEMPLATE_CODE`를 새 `cute-enough-ticket-recharged-v2`로 설정했다. 알림 암호화 키·cron secret과 일치하는 Vault secret, worker URL을 준비했다. 비밀값은 서버와 무시된 로컬 env에만 보관했다. 인증한 worker 확인 요청은 HTTP `200`, `enabled: false`를 반환했다.
- **광고·공유·알림의 운영 스위치는 모두 `false`다. 실제 푸시·친구 초대는 보내지 않았다.** 동의문 명칭 수정과 QR 동의·수신 검증, 공유 모듈 확인 및 광고·다중 초대 실기기 검증이 남아 있다. 신규 사용자 대상 일반 광고성 푸시는 별도 단계이며 MCP로 생성할 수 없어 콘솔 웹 준비가 필요하다.
- AIT 통합·화면·API 사용자 안내 문구 수정은 다른 작업 창에 전달했다. 사용자에게 보이는 명칭은 `티켓`, 수량은 `장`으로 통일하되 API·DB 식별자는 유지한다. 이번 콘솔·서버 준비를 새 AIT 생성·업로드·심사·출시 완료로 기록하지 않는다.

## 순차 실행 시작 — 2026-09-05 22:38 KST

- 사용자 지시에 따라 MCP 연결 확인부터 광고·공유·알림 순서로 진행한다. 미리보기·썸네일은 사용자가 별도로 진행하며 이 작업 범위에서 제외한다. 각 작업 창은 독립적으로 진행하고 별도 요청 없는 대화·진행 상황 전달을 중단한다.
- `codex mcp login apps-in-toss-console`은 성공했지만, 이 대화의 콘솔 MCP 실조회는 재시도에서도 `Auth required`를 반환했다. 재로그인과 연결 복구를 구분하며 1단계 실조회 완료로 처리하지 않는다. 앱의 MCP 연결 재시작 확인이 남아 있다.
- 현재 승인된 v2 문구에 맞춰 `notification-policy.ts`의 제목·본문, `notification-settings.ts`의 오류 안내 총 3곳을 `티켓`으로 정리했다. 알림 정책·설정 기존 Vitest 18/18, 광고·공유·복구·서버 보상 기존 Vitest 22/22 통과. 변경은 로컬이며 이 창에서 Edge 재배포하지 않았다.
- 새 알림 카피와 별도 광고성 방문 초대는 아직 미등록이다. 이번 실행에서 기능 스위치·secret·템플릿·승인 대기견은 변경하지 않았고, 실제 푸시·친구 초대도 보내지 않았다.

## MCP 복구 및 공유 QR 연결 — 2026-09-05

- 앱과 같은 Codex 0.153.4 실행 파일로 재인증한 후 새 대화 턴에서 콘솔 MCP 실제 조회가 복구됐다. 광고·알림·9월 5일 DAU 조회 성공으로 확인했으며 DAU는 120명이다.
- 광고 슬롯 `ait.v2.live.285b758f6d564708`은 활성이고 현재 표시 단위는 `강아지 티켓` 1장이다. 광고 서버 DB 스위치는 `false`다. 업로드된 `20260905-23`은 광고 OFF 후보이므로 광고 ON AIT가 별도로 필요하다.
- 공유 MCP 목록·개별 조회는 여전히 `존재하지 않는 정보`를 반환했다. 사용자가 정확한 UUID의 존재·활성·단위·친구당 1장을 콘솔에서 확인했고, 운영 DB UUID도 일치함을 조회했다.
- 공유 QR 검증을 위해 Edge `AIT_SHARE_REWARDS_ENABLED=true`, DB `pet_reward_settings.share_enabled=true`를 적용하고 Git 무시 대상인 로컬 백업의 동일 플래그도 맞췄다. 다른 secret 값은 변경하지 않았다. 광고·알림 활성화와 구분한다.
- 적용 전 공유 세션·이벤트·보너스 잔액·알림 신청·작업은 모두 0이었다. 이 작업은 보너스를 직접 지급하지 않았으며, 실제 초대 화면·취소·지급 검증은 사용자 휴대폰 확인이 남아 있다. 승인 7건·대기 6건·반려 2건을 읽기 전용으로 확인했다.
- 콘솔의 구형 알림 `11799`와 새 알림 `11801`은 모두 활성 상태였고, 서버 연결은 v2 하나다. 사용자의 구형 알림 정리 및 동의문 `120468` 명칭 수정 확인이 남아 있다. 알림 발송은 계속 꺼져 있다.
- Edge 설정은 조회 API가 `value` 필드로 반환하는 SHA-256 지문을 기대값과 비교해 확인했다. 공유 `true`, 광고·알림 `false`, templateCode `cute-enough-ticket-recharged-v2`가 모두 일치한다. 실제 비밀값은 출력하지 않았다.
