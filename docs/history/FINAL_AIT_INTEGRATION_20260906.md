# 2026-09-06 최종 운영 설정 통합 AIT

> 과거 구현·배포 기록입니다. 당시 상태·명령은 현재 운영 절차가 아닙니다. [문서 안내](../README.md)에서 현재 가이드를 확인하세요. 도감 초기화·승인·출시는 별도의 명시적 확인 없이 실행하지 않습니다.

## 전달 파일

- `TO_UPLOAD/cute-enough-20260906-integrated-ads-on.ait` (저장 시점 고정본)
- 루트 `cute-enough.ait`도 같은 파일. 이후 빌드로 덮일 수 있으므로 전달은 고정본 권장.
- deploymentId: `01a07610-7ed6-750f-a610-1e9820a38c6d`
- 2,708,314 bytes / SHA-256 `6755769a732014b73123661d8d6191f84a4e9627553541d6045b02858ac11a55`
- Node 24.14.1 / SDK 3.1.1 / runtime production
- 광고 ON / 광고 테스트 모드 OFF / `ait.v2.live.285b758f6d564708`
- 소스 커밋: `a20041e5334bc8d57e41a18d72388f62b56f754b`
- 문서 작성 전 미커밋 변경 식별값(2026-09-06 18:35:51 KST): `27b646d9835f59eed235f878969e87871f54a68e026343c7f87a5e78acbdeb77`
- 미추적 파일 106개 포함 해시. 기존 작업을 초기화하거나 되돌리지 않았음.

## 앱에 포함한 변경

- 간식 → 쓰다듬기/터치 3회 → 사진. 한 제스처당 하트 하나.
- 누적 이동 72px 이상 stroke, 짧은 움직임 tap, 키보드 keyboard 분석 분류.
- 매 입력 450ms 반응: 작은 점프/눈웃음/꼬리. 완료도 450ms 뒤 한 번.
- 취소된 긴 제스처는 차감/하트 없음. 길게 누른 키의 반복, 포인터 중 키보드, 보조 포인터, 취소된 간식 드롭 방지.
- `PetDesignV1`/`PetDesignSvg` 확정 SVG 유지: 집·교감·공유·내 강아지·앨범. 공개 외형을 ID별 다시 생성하지 않음.
- 이름 필수, 신규 등록 사진 1~5장, 기존 강아지의 남은 한도 내 사진 추가 접수.
- 기존 강아지/사진/주인 원래 선택/수집/하트 기록 보존.
- 앨범 사진 수, 사진별 설명, 기존 사진창 확대와 하단 돌아가기 UI.
- 최신 티켓·공유 보상·충전 안내. 광고 완료 권한 흐름 유지.

## 추가 사진 검수 (AIT와 별도인 PC 검수 서버)

- 실행: 프로젝트에서 `npm run review` → `http://127.0.0.1:4178/review`
- 상단 `추가 사진 검수`에서 접수 묶음별 사진 확인.
- 이미지 로딩 후 사진별 `사진 N 확인`을 모두 체크 → `추가 사진 승인` → `확정하기`.
- 반려는 사유 입력 → `추가 사진 반려` → `확정하기`.
- 이번 구현은 접수 묶음 전체 승인/반려. 기존 공개 사진의 순서·설명·제외는 기존 사진 관리에서 별도 수정.
- 승인 시 기존 대표 사진 뒤에 추가하며 SVG/이름/기존 사진 ID/앨범 권한은 변경하지 않음.
- pending 강아지의 추가 사진 승인은 강아지 자체의 공개 승인이 아님.
- 검수 API는 로컬 동일 출처와 서버 전용 키를 사용하며, 앱 번들에 관리 화면/비밀값을 넣지 않음.
- RPC는 service-role 전용. 원본 존재·사진 한도·검수 시점 변경·중복 결정 검사 및 감사 기록.
- 원본 파일과 반려 접수 이력도 삭제하지 않음.

## 운영 반영과 검증

- `20260906000700_review_photo_additions.sql`만 운영 DB 적용 및 migration 이력 기록.
- 적용 전후 pets/pet_photos/추가 접수/추가 사진 항목 해시 동일 확인. 개별 승인 RPC는 실행하지 않음.
- 기존 Edge `pet-api` v28 그대로 ACTIVE. 이번 변경에 Edge 코드 재배포 필요 없음.
- 운영 설정 ads_enabled=true, share_enabled=true 확인. 다른 운영 스위치 변경 없음.
- 승인견 12마리 모두 published_design_id 존재. 추가 사진 대기 묶음 현재 0건.
- 로컬 검수 추가 사진 조회 실제 서버 연결 HTTP 200 확인.
- 전체 Vitest 98개 파일/947개 테스트 통과, 타입 검사 통과.
- 검수 통합 Node 테스트 68개 통과. 별도 운영 빌드 설정 테스트 22개 통과.
- production 웹/AIT 빌드 통과. 실제 AIT 15개 웹 텍스트 파일이 현재 dist와 일치.
- 실제 바이너리에서 production 런타임, 광고 ON/실광고 ID/테스트 OFF, 운영 URL/공개키 포함 확인.
- 샘플 실사 5장 및 개발 도구 미포함. 이전 잘못된 3.06MiB AIT는 새 검증기로 거부됨.
- 재검증: `node scripts/verify-production-ait.mjs TO_UPLOAD/cute-enough-20260906-integrated-ads-on.ait`
  (동일 빌드의 dist와 비교하므로 다른 빌드 후에는 기존 스냅샷과 구분해야 함.)

## 아직 구분해야 할 상태

- 완료: 코드 구현, 로컬 자동 테스트, 운영 DB RPC 적용, 최종 AIT 생성/실파일 검증.
- 이 요청에서는 Toss 콘솔 업로드·테스트 푸시·검토 요청·일반 사용자 출시를 하지 않았음.
- 휴대폰 실제 광고 재생·추가 사진 접수→운영자 확정→타인 수집 전 과정은 미검증.
- 실광고 ID로 자동 광고 조회/보상 적립 테스트를 실행하지 않음.
- 빌드 경고: 메인 JS 약 456KB gzip으로 이전 목표 350KB보다 큼. 이번 기능 통합의 출시 차단 오류는 아니지만 성능 개선 과제로 남음.
