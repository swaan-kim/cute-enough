import type { ReactNode } from 'react';

export function TDSMobileAITProvider({ children }: { children: ReactNode; brandPrimaryColor?: string; fontScaleAvailable?: boolean }) {
  return <>{children}</>;
}
